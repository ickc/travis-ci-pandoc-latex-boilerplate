\chapterstyle{veelo}
\pagestyle{ruled}
\hypersetup{bookmarksnumbered,pdfpagelabels,pagebackref,hypertexnames=false,linktocpage=true}
% \usepackage{wrapfig}

% for alphbetical lists
% \usepackage{enumitem}
% \setlist[enumerate,1]{label=(\alph*)}
% \setlist[enumerate,2]{label=(\roman*)}
% \setlist[enumerate,3]{label=(\Alph*)}
% \setlist[enumerate,4]{label=(\Roman*)}
% \setlist[enumerate,5]{label=(\arabic*)}

% enforce only amsmath environments (and prohibit plain TeX environments)
% \usepackage[all, error]{onlyamsmath}

% reset counter per part: e.g. lab start with lab 1
\makeatletter
\@addtoreset{chapter}{part}
\makeatother
